# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  host: localhost
  database: postgres
  username: postgres
  password: whizium

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  host: localhost
  database: test
  username: postgres
  password: whizium

# Heroku automatically generates a Database.yml file. Would prefer below is correct, but can live with this for now.
production:
  adapter: postgresql
  database: db/production.sqlite3
  pool: 5
  timeout: 5000
  username: postgres
  password: whizium
